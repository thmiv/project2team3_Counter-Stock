<div class="container">
    <div class="row">
        <div class="col-8 offset-2">
            <div class="card mt-4">
                <div class="card-body">
                    <p>
                        <strong>You: </strong>{{this.character.username}}</p>
                    <p>
                        <strong>Stock: </strong> {{this.character.stockChoice}}</p>
                    <p id="player" data-price="{{this.character.stockPrice}}">
                        <strong>Stock price: </strong>${{this.character.stockPrice}}</p>
                    <a href="/">← Back To Home</a>
                </div>
                <button id="fight" class="btn btn-primary float-right">FIGHT!</button>
                <div class="card-body">
                    <p>
                        <strong>Them: </strong>{{this.opponent.username}}</p>
                    <p>
                        <strong>Stock: </strong> {{this.opponent.stockChoice}}</p>
                    <p id="opponent" data-price="{{this.opponent.stockPrice}}">
                        <strong>Stock price: </strong>${{this.opponent.stockPrice}}</p>
                </div>
            </div>
        </div>
    </div>
</div>

<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
<script src="/js/fight.js"></script>